---
export interface Props {
  stacks: {
    category: string;
    technologies: { name: string; level: number; icon?: string }[];
  }[];
}

const { stacks } = Astro.props;
---

<section id="skills" class="py-20 bg-white">
  <div class="max-w-6xl mx-auto px-6 lg:px-8">
    <div class="text-center mb-16 animate-fade-in">
      <h2 class="text-4xl lg:text-5xl font-bold text-gray-900 mb-6">
        Ma <span class="text-primary-600">stack</span> technique
      </h2>
      <p class="text-xl text-gray-600 max-w-2xl mx-auto">
        Technologies et outils que je maîtrise pour créer des expériences digitales exceptionnelles
      </p>
    </div>

    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
      {stacks.map((stack, index) => (
        <div class="bg-gray-50 rounded-2xl p-8 hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2 animate-slide-up" style={`animation-delay: ${index * 100}ms`}>
          <h3 class="text-2xl font-bold text-gray-900 mb-6 text-center">{stack.category}</h3>

          <div class="space-y-4">
            {stack.technologies.map((tech) => (
              <div class="flex items-center justify-between">
                <div class="flex items-center space-x-3">
                  {tech.icon && (
                    <div class="w-8 h-8 bg-primary-100 rounded-lg flex items-center justify-center">
                      <span class="text-primary-600 text-sm font-bold">{tech.icon}</span>
                    </div>
                  )}
                  <span class="font-medium text-gray-900">{tech.name}</span>
                </div>

                <!-- Skill level bars -->
                <div class="flex space-x-1">
                  {Array.from({ length: 5 }, (_, i) => (
                    <div class={`w-2 h-2 rounded-full ${
                      i < tech.level
                        ? 'bg-primary-500'
                        : 'bg-gray-300'
                    }`}></div>
                  ))}
                </div>
              </div>
            ))}
          </div>
        </div>
      ))}
    </div>

    <!-- CTA Section -->
    <div class="text-center mt-16 animate-fade-in">
      <div class="bg-gradient-to-r from-primary-500 to-secondary-500 rounded-2xl p-8 lg:p-12 text-white">
        <h3 class="text-3xl font-bold mb-4">Prête à collaborer</h3>
        <p class="text-xl text-primary-100 mb-8 max-w-2xl mx-auto">
          Vous avez un projet en tête ? Discutons de comment nous pouvons le réaliser ensemble.
        </p>
        <a
          href="#contact"
          class="bg-white text-primary-600 px-8 py-4 rounded-full font-semibold text-lg hover:bg-gray-100 transform hover:-translate-y-1 transition-all duration-300 shadow-lg hover:shadow-xl inline-block"
        >
          Démarrons un projet
        </a>
      </div>
    </div>
  </div>
</section>